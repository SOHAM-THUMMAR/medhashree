# Google Auth
VITE_GOOGLE_CLIENT_ID=your_google_auth_client_id

# Backend API URL (for Cloudflare Pages / separate hosting)
# Leave blank to use relative routing when served from the same domain
VITE_API_URL=https://your-backend-vps-domain.com